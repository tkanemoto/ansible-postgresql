---
# vars file for postgresql (Ubuntu specific)

postgresql_packages:
  - postgresql
  - libpq-dev
  - python-psycopg2
postgresql_configuration_directory: /etc/postgresql/{{ postgresql_version }}/main
